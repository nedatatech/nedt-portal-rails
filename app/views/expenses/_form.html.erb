<%= form_with(model: expense, local: true) do |form| %>
  <% if expense.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expense.errors.count, "error") %> prohibited this expense from being saved:</h2>

      <ul>
      <% expense.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :owner_id %>
    <%= form.text_field :owner_id, id: :expense_owner_id %>
  </div>

  <div class="field">
    <%= form.label :recurring %>
    <%= form.check_box :recurring, id: :expense_recurring %>
  </div>

  <div class="field">
    <%= form.label :due %>
    <%= form.date_select :due, id: :expense_due %>
  </div>

  <div class="field">
    <%= form.label :pay_to_id %>
    <%= form.text_field :pay_to_id, id: :expense_pay_to_id %>
  </div>

  <div class="field">
    <%= form.label :pay_from_id %>
    <%= form.text_field :pay_from_id, id: :expense_pay_from_id %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount, id: :expense_amount %>
  </div>

  <div class="field">
    <%= form.label :status_id %>
    <%= form.text_field :status_id, id: :expense_status_id %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :expense_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
