<%= form_for @item_location, :url => {:action => "move_to_truck"}, :html => { :method => :patch } do |form| %>

  <% if @item_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item_location.errors.count, "error") %> prohibited this info from being saved:</h2>

      <ul>
      <% @item_location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    
  <div class="field">
    <%= form.label :select_itemz_location %>  
    <%= form.collection_select(:id, @item_locations, :id, :name) %>   

    <%= hidden_field_tag 'inventory_item_id',  params[:inventory_item_id]%>        
  </div>

  <div class="actions">
    <%= form.submit "Select location" %>
  </div>
<% end %>
