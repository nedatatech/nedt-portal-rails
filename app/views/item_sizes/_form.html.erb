<%= form_with(model: item_size, local: true) do |form| %>
  <% if item_size.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item_size.errors.count, "error") %> prohibited this item_size from being saved:</h2>

      <ul>
      <% item_size.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :item_size_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :item_size_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
