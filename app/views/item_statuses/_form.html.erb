<%= form_with(model: item_status, local: true) do |form| %>
  <% if item_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item_status.errors.count, "error") %> prohibited this item_status from being saved:</h2>

      <ul>
      <% item_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :item_status_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :item_status_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
