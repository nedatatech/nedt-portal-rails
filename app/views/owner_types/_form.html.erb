<%= form_with(model: owner_type, local: true) do |form| %>
  <% if owner_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(owner_type.errors.count, "error") %> prohibited this owner_type from being saved:</h2>

      <ul>
      <% owner_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :owner_type_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :owner_type_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
