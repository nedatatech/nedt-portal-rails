<%= form_with(model: status_datum, local: true) do |form| %>
  <% if status_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(status_datum.errors.count, "error") %> prohibited this status_datum from being saved:</h2>

      <ul>
      <% status_datum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :status_datum_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :status_datum_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
