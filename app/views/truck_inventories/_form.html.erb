<%= form_with(model: truck_inventory, local: true) do |form| %>
  <% if truck_inventory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(truck_inventory.errors.count, "error") %> prohibited this truck_inventory from being saved:</h2>

      <ul>
      <% truck_inventory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :inventory_item_id %>  
    <%= form.collection_select(:inventory_item_id, InventoryItem.all, :id, :name) %> 
  </div>

  <div class="field">
    <%= form.label :item_location_id %>
    <%= form.collection_select(:item_location_id, ItemLocation.all, :id, :name) %> 
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
